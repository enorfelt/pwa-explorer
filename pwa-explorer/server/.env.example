PORT=3000
CORS_ORIGIN=http://localhost:4200
VAPID_EMAIL=admin@pwa-explorer.example.com
# Generate VAPID keys by running the server once without these set,
# then copy the printed values here:
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
